[package]
name = "openstream-sync"
description = "Public operation log and merge rules (field-level LWW by hybrid logical clock, tombstones, needs_resolution). Cloud-agnostic by contract."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true
