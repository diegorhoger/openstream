# Continue OpenStream

Read `AGENTS.md`, `.opencode/graph.yaml`, the roadmap, open PRs, and open issues.

Resume the oldest open PR. If none exists, select the lowest-numbered issue whose dependencies are merged. Post/update the durable agent status, create the role graph, execute the bounded loop, and stop only at the exact-head human review gate or a documented hard stop.
